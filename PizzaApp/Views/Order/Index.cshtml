@*@model List<Order>

    <h2>Made orders:</h2>
    <ol>
    @for(int i = 0; i < @Model.Count; i++)
    {
        <li>@Model.ElementAt(i).Id - @Model.ElementAt(i).Pizza.Name  - User: @Model.ElementAt(i).User.FirstName</li>
    }
</ol>*@

@model List<OrderDetailsViewModel>

    <h2>It is the following pizzas ordered:</h2>
    <ul>
    @for(int i=0; i<Model.Count; i++)
    {
        <li>@Model.ElementAt(i).PizzaName for @Model.ElementAt(i).UserFullName costs @Model.ElementAt(i).Price 

            And this pizza should be delivered on address @Model.ElementAt(i).UserAddress
        </li>
    }
</ul>
