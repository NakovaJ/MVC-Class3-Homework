@model List<User>
<h2>These are th following users in our system:</h2>
@for(int i=0; i<Model.Count;i++){
    <ol>
        <li> @("Mr./Ms." + Model[i].FirstName +" "+ Model[i].LastName)</li>
    </ol>
}
